import { Inter, Lustria } from 'next/font/google'
import { Lusitana } from 'next/font/google';

export const lusitana = Lustria({weight: '400', subsets: ['latin']});
export const inter = Inter({subsets: ['latin']});